新增“画质增强”页面，按需准备并打开 DLSS5-Swapper 2.2.7 与 DLSS 5 Visual Enhancer 8.0，支持固定版本下载、本地导入、取消、准备状态和中文教程。游戏详情可带入目录，选择 EXE 后只读查看 32/64 位、静态图形接口与已有 Mod 线索，并复制路径到外部管理器使用。

保留 0.2 的扫描修复、默认 Native 0.2.3 / 可选 0.2.4、2×/3×/4×上限、后端升级/降级与原件恢复。外部工具的实际效果参数沿用各自界面；不自动修改驱动或向游戏安装新组件，主程序包不捆绑两个大型外部包。

**下载：** 下方 `DLSS-Swapper-FG-Preview-0.3-win-x64.zip`，完整解压到新目录运行 `DLSS Swapper.exe`。两版均退出后，可复制旧 `StoredData-FG-Preview` 沿用设置；保留旧版本用于回退。新版附 `README-FG-Preview.md` 与 `Enhancement-Tools.md`。

**先试：** 左侧“画质增强” → 选择需要的工具 → 下载并准备 / 导入 → 打开。建议先在 RTX 3080 试一款游戏或一段短视频。40HX 的 NR、v8.0 显卡名称检测和实际兼容性仍待验证。

验证包括 Windows 核心检查、WinUI x64 构建和便携包结构；真实固定工具包已在本机核对并准备，未执行第三方程序。Windows UI、GPU、游戏画质/帧率和视频效果待用户实测。“已准备”不表示 DLSS 5 已在游戏中生效。
